- Alsa beachten [OK]
Besser: - Verändertes in /etc kopieren [OK]
	- Verändertes in /home kopieren. [OK]

- KDE-Sprache setzen. (unnötig, wenn /home kopiert wird) [OK]

- Konfiguration nach Erstellen sichern [OK]

- Bei really_quit $BT->"$BT" [OK]

- Knoppix vs. Debian [OK]

- UND: Fragen ob die Konfiguration gespeichert werden soll! [OK]

- rebuildfstab auf neutralen Nutzer setzen ! [OK]

- sources.list auf us anpassen. 

- grub verwenden.
